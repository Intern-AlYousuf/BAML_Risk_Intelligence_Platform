# ── Core Framework ────────────────────────────────────────────────────────────
fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.10.3
pydantic-settings==2.6.1

# ── Environment & Config ──────────────────────────────────────────────────────
python-dotenv==1.0.1

# ── Auth & Security ───────────────────────────────────────────────────────────
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.18

# ── HTTP Clients (market data, Yahoo Finance, FRED API) ───────────────────────
httpx==0.28.1
aiohttp==3.11.10

# ── Database (SQLAlchemy async engine + asyncpg driver) ───────────────────────
# Required at import time even when no DB is provisioned — the engine is lazy
# and does not connect until the first query is executed.
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0

# ── Data Processing & Forecasting ─────────────────────────────────────────────
numpy==2.2.0
pandas==2.2.3
statsmodels==0.14.4

# ── Caching ───────────────────────────────────────────────────────────────────
cachetools>=5.3.0

# ── Logging ───────────────────────────────────────────────────────────────────
structlog==24.4.0
